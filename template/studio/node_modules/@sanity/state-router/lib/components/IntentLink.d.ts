import React from 'react';
import Link from './Link';
import { RouterProviderContext, IntentParameters } from './types';
import internalRouterContextTypeCheck from './internalRouterContextTypeCheck';
interface IntentLinkProps {
    intent: string;
    params?: IntentParameters;
}
export default class IntentLink extends React.PureComponent<IntentLinkProps & Omit<React.HTMLProps<HTMLAnchorElement>, 'ref'>> {
    context: RouterProviderContext | null;
    static contextTypes: {
        __internalRouter: typeof internalRouterContextTypeCheck;
    };
    _element: Link | null;
    focus(): void;
    setElement: (element: Link | null) => void;
    resolveIntentLink(intent: string, params?: IntentParameters): string;
    render(): JSX.Element;
}
export {};
