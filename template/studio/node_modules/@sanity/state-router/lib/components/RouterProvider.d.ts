import React from 'react';
import PropTypes from 'prop-types';
import { Router } from '../types';
import { InternalRouter, NavigateOptions, RouterProviderContext, RouterState } from './types';
declare type Props = {
    onNavigate: (nextPath: string, options?: NavigateOptions) => void;
    router: Router;
    state: RouterState;
    children: React.ReactNode;
};
export default class RouterProvider extends React.Component<Props> {
    static childContextTypes: {
        __internalRouter: PropTypes.Requireable<object>;
    };
    __internalRouter: InternalRouter;
    _state: RouterState;
    constructor(props: Props);
    navigateUrl: (url: string, options?: NavigateOptions) => void;
    navigateState: (nextState: Record<string, any>, options?: NavigateOptions) => void;
    getState: () => Record<string, any>;
    resolvePathFromState: (state: Record<string, any>) => string;
    navigateIntent: (intentName: string, params?: Record<string, any> | [Record<string, any>, Record<string, any>] | undefined, options?: NavigateOptions) => void;
    resolveIntentLink: (intentName: string, parameters?: Record<string, any> | [Record<string, any>, Record<string, any>] | undefined) => string;
    getChildContext(): RouterProviderContext;
    UNSAFE_componentWillReceiveProps(nextProps: Props): void;
    render(): JSX.Element;
}
export {};
