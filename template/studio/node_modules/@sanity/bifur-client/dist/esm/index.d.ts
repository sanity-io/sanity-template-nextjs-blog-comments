import { SanityClient } from '@sanity/client';
import { BifurClient } from './types';
interface Options {
    timeout?: number;
}
export { BifurClient };
export declare function fromUrl(url: string, options?: Options): BifurClient;
export declare function fromSanityClient(client: SanityClient): BifurClient;
