import { Observable } from 'rxjs';
export declare type RequestMethod = 'query' | 'mutate' | 'presence_rollcall' | 'presence_announce' | 'presence_disconnect';
export declare type SubscribeMethods = 'presence' | 'listen';
export declare type RequestParams = Record<string, any>;
export declare type JSONRpcMessage<T> = {
    jsonrpc: string;
    id: string;
    method: string;
    params: RequestParams;
    result: T;
};
export interface BifurClient {
    heartbeats: Observable<Date>;
    request: <T>(method: RequestMethod | SubscribeMethods, params?: RequestParams) => Observable<T>;
}
