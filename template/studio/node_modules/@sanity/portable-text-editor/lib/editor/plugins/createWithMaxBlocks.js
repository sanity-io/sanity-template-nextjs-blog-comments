"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createWithMaxBlocks = void 0;
/**
 * This plugin makes sure that the PTE maxBlocks prop is respected
 *
 */
function createWithMaxBlocks(rows) {
    return function withMaxBlocks(editor) {
        const { apply } = editor;
        editor.apply = operation => {
            if (rows > 0 && editor.children.length >= rows) {
                if ((operation.type === 'insert_node' || operation.type === 'split_node') &&
                    operation.path.length === 1) {
                    return;
                }
            }
            apply(operation);
        };
        return editor;
    };
}
exports.createWithMaxBlocks = createWithMaxBlocks;
//# sourceMappingURL=createWithMaxBlocks.js.map