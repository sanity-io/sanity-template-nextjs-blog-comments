"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isBuilder = isBuilder;
exports.resolveInitialValue = resolveInitialValue;

var _isPlainObject2 = _interopRequireDefault(require("lodash/isPlainObject"));

var _validate = require("./validate");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function isBuilder(template) {
  return typeof template.serialize === 'function';
}

function resolveInitialValue(_x) {
  return _resolveInitialValue.apply(this, arguments);
}

function _resolveInitialValue() {
  _resolveInitialValue = _asyncToGenerator(function* (template) {
    var params = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};

    // Template builder?
    if (isBuilder(template)) {
      return resolveInitialValue(template.serialize(), params);
    }

    var id = template.id,
        value = template.value;

    if (!value) {
      throw new Error("Template \"".concat(id, "\" has invalid \"value\" property"));
    } // Static value?


    if ((0, _isPlainObject2.default)(value)) {
      return (0, _validate.validateInitialValue)(value, template);
    } // Not an object, so should be a function


    if (typeof value !== 'function') {
      throw new Error("Template \"".concat(id, "\" has invalid \"value\" property - must be a plain object or a resolver function"));
    }

    var resolved = yield value(params);
    return (0, _validate.validateInitialValue)(resolved, template);
  });
  return _resolveInitialValue.apply(this, arguments);
}