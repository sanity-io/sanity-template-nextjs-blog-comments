"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = resolveListComponents;

var _default = require("part:@sanity/components/lists/default");

var _sortable = require("part:@sanity/components/lists/sortable");

var _grid = require("part:@sanity/components/lists/grid");

var _sortableGrid = require("part:@sanity/components/lists/sortable-grid");

function resolveListComponents(isSortable, isGrid) {
  if (isSortable) {
    return isGrid ? {
      List: _sortableGrid.List,
      Item: _sortableGrid.Item
    } : {
      List: _sortable.List,
      Item: _sortable.Item
    };
  }

  return isGrid ? {
    List: _grid.List,
    Item: _grid.Item
  } : {
    List: _default.List,
    Item: _default.Item
  };
}