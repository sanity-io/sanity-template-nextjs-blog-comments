"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getPreviewSnapshot = getPreviewSnapshot;
exports.search = search;

var _operators = require("rxjs/operators");

var _client = _interopRequireDefault(require("part:@sanity/base/client"));

var _weighted = require("part:@sanity/base/search/weighted");

var _preview = require("part:@sanity/base/preview");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getPreviewSnapshot(value, referenceType) {
  return (0, _preview.observeForPreview)(value, referenceType).pipe((0, _operators.map)(result => result.snapshot));
}

function search(textTerm, referenceType, options) {
  var doSearch = (0, _weighted.createWeightedSearch)(referenceType.to, _client.default, options);
  return doSearch(textTerm, {
    includeDrafts: false
  }).pipe((0, _operators.map)(results => results.map(res => res.hit)));
}