import { Observable } from 'rxjs';
import { AssetSourceSpec, SchemaType } from '@sanity/types';
import { Patch } from '../../typedefs/patch';
export declare type UploadEvent = {
    type: 'uploadEvent';
    patches: Patch[] | null;
};
export declare type ResolvedUploader = {
    uploader: Uploader;
    type: SchemaType;
};
export declare type UploadOptions = {
    metadata?: string[];
    storeOriginalFilename?: boolean;
    label?: string;
    title?: string;
    description?: string;
    creditLine?: string;
    source?: AssetSourceSpec;
};
export declare type UploaderDef = {
    type: string;
    accepts: string;
    upload: (file: File, type: SchemaType) => Observable<UploadEvent>;
};
export declare type Uploader = {
    type: string;
    accepts: string;
    upload: (file: File, type: SchemaType, options?: UploadOptions) => Observable<UploadEvent>;
    priority: number;
};
export declare type UploaderResolver = (type: SchemaType, file: File) => Uploader | null;
