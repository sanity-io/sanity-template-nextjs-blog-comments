"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getValidationRule = getValidationRule;

// Note: consider this "internals"
function getValidationRule(type, ruleName) {
  if (!type || !type.validation || !type.validation.length) {
    return null;
  }

  for (var i = 0; i < type.validation.length; i++) {
    var validation = type.validation[i];

    if (!validation || !validation._rules) {
      continue;
    }

    for (var r = 0; r < validation._rules.length; r++) {
      var rule = validation._rules[r];

      if (rule.flag === ruleName) {
        return rule;
      }
    }
  }

  return null;
}