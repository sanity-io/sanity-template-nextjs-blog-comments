import { SchemaType } from '@sanity/types';
interface Rule {
    _rules: RuleSpec[];
}
interface RuleSpec {
    flag: string;
    constraint?: unknown;
}
export declare function getValidationRule<T = SchemaType>(type: T & {
    validation?: Rule[];
}, ruleName: string): RuleSpec | null;
export {};
