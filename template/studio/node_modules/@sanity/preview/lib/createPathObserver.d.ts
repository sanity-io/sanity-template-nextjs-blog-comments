import { FieldName, Path, Value } from './types';
declare type ObserveFieldsFn = (id: string, fields: FieldName[]) => any;
export default function createPathObserver(observeFields: ObserveFieldsFn): (value: Value, paths: Path[]) => any;
export {};
