import { MenuItem } from '@sanity/components';
import { DeskToolFeatures } from '../../../../features';
import { Doc } from '../../types';
interface Params {
    features: DeskToolFeatures;
    isHistoryOpen?: boolean;
    rev: string | null;
    value: Doc | null;
}
export declare const getProductionPreviewItem: ({ value, rev }: Params) => MenuItem | null;
export declare const getMenuItems: (params: Params) => MenuItem[];
export {};
