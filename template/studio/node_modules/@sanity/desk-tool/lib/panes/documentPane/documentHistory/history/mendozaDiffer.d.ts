import { Chunk, Annotation } from '@sanity/field/diff';
import { Diff } from '@sanity/diff';
import { Value } from 'mendoza/lib/incremental-patcher';
import { Timeline } from './timeline';
export declare type Meta = {
    chunk: Chunk;
    transactionIndex: number;
} | null;
export declare type AnnotationExtractor = {
    fromValue(value: Value<Meta>): Annotation;
    fromMeta(meta: Meta): Annotation;
};
export declare function diffValue(timeline: Timeline, firstChunk: Chunk | null, from: Value<Meta>, fromRaw: unknown, to: Value<Meta>, toRaw: unknown): Diff<Annotation>;
