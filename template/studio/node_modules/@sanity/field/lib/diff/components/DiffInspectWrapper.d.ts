import React from 'react';
import { FieldChangeNode } from '../../types';
interface Props {
    children: React.ReactNode;
    change: FieldChangeNode;
    className: string;
}
export declare function DiffInspectWrapper({ children, className, change }: Props): React.ReactElement;
export {};
