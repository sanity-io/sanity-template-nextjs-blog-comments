import { Path } from '@sanity/types';
import React from 'react';
import { ObjectDiff, ObjectSchemaType } from '../../../../diff';
import { PortableTextChild } from '../types';
interface AnnotationProps {
    diff?: ObjectDiff;
    object: PortableTextChild;
    children: JSX.Element;
    schemaType?: ObjectSchemaType;
    path: Path;
}
export declare function Annotation({ children, diff, object, schemaType, path, ...restProps }: AnnotationProps & Omit<React.HTMLProps<HTMLSpanElement>, 'onClick'>): JSX.Element;
export {};
