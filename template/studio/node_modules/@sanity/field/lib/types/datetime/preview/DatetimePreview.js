"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.DatetimePreview = void 0;

var _react = _interopRequireDefault(require("react"));

var _format = _interopRequireDefault(require("date-fns/format"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var DatetimePreview = function DatetimePreview(_ref) {
  var value = _ref.value,
      schemaType = _ref.schemaType;
  return /*#__PURE__*/_react.default.createElement(_react.default.Fragment, null, formatDate(value, schemaType));
};

exports.DatetimePreview = DatetimePreview;

function formatDate(value, schemaType) {
  var options = schemaType.options,
      name = schemaType.name;
  var date = new Date(value);
  var dateFormat = 'yyyy-MM-dd';

  if (options && options.dateFormat) {
    dateFormat = options.dateFormat;
  }

  if (name === 'date') {
    // If the type is date only
    return (0, _format.default)(date, dateFormat);
  } // If the type is datetime


  if (options && options.timeFormat) {
    dateFormat += " ".concat(options.timeFormat);
  } else {
    dateFormat += ' HH:mm';
  }

  return (0, _format.default)(date, dateFormat);
}