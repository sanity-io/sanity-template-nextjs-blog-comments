import { Marker } from '@sanity/types';
interface ValidationStatus {
    isValidating: boolean;
    markers: Marker[];
}
export declare function useValidationStatus(publishedDocId: string, docTypeName: string): ValidationStatus;
export {};
