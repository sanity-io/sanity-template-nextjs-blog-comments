/// <reference types="react" />
import { AvatarPosition, AvatarSize, AvatarStatus } from 'part:@sanity/components/avatar';
import { User } from '../datastores/user/types';
interface BaseProps {
    position?: AvatarPosition;
    animateArrowFrom?: AvatarPosition;
    size?: AvatarSize;
    status?: AvatarStatus;
    tone?: 'navbar';
}
export declare type Props = BaseProps & UserProps;
interface LoadedUserProps extends BaseProps {
    user: User;
}
interface UnloadedUserProps extends BaseProps {
    userId: string;
}
declare type UserProps = LoadedUserProps | UnloadedUserProps;
export declare function UserAvatar(props: Props): JSX.Element;
export {};
