import { Observable } from 'rxjs';
import { CommitFunction, DocumentMutationEvent, DocumentRebaseEvent, MutationPayload, SnapshotEvent, RemoteSnapshotEvent } from './types';
import { ListenerEvent } from '../getPairListener';
export declare const createObservableBufferedDocument: (listenerEvent$: Observable<ListenerEvent>, commitMutations: CommitFunction) => {
    updates$: Observable<SnapshotEvent | DocumentRebaseEvent | DocumentMutationEvent>;
    consistency$: Observable<boolean>;
    remoteSnapshot$: Observable<RemoteSnapshotEvent>;
    addMutation: (mutation: MutationPayload) => void;
    addMutations: (mutations: MutationPayload[]) => void;
    commit: () => Observable<never>;
};
