"use strict";

// Figure out which locale the user prefers.
// Return a promise in order to support asynchronous resolution.
var nav = typeof navigator === 'undefined' ? {} : navigator;
var defaultLanguage = 'en-US';

function resolveLanguage() {
  var supportedLanguages = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
  var language = nav.language || nav.browserLanguage || defaultLanguage;

  if (supportedLanguages && !supportedLanguages.includes(language)) {
    language = supportedLanguages[0];
  }

  return Promise.resolve(language);
}

module.exports = {
  resolveLanguage
};